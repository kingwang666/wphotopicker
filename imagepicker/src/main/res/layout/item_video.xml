<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.wang.imagepicker.widget.SquareImageView
        android:id="@+id/video_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="1dip"
        android:scaleType="centerCrop"/>

    <View
        android:id="@+id/masker_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/percent50BlackColor"/>

    <ImageView
        android:id="@+id/selected_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="16dp"
        android:paddingStart="16dp"
        android:layout_gravity="right"
        android:src="@drawable/selector_item_checked"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/video_bottom_alpha"
        android:paddingBottom="2dp"
        android:paddingTop="4dp">

        <TextView
            android:id="@+id/duration_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|left"
            android:drawablePadding="4dp"
            android:maxEms="6"
            android:paddingLeft="4dp"
            android:textAppearance="@style/TextAppearance.App.Medium"
            android:textColor="@color/white"/>

        <TextView
            android:id="@+id/size_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|right"
            android:maxEms="4"
            android:paddingRight="4dp"
            android:textAppearance="@style/TextAppearance.App.Medium"
            android:textColor="@color/white"/>

    </FrameLayout>

</FrameLayout>